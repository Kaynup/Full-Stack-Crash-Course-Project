# Frontend
Three input fields (Name, Role, Email) with Submit button using React `useState` to track form values.

## Submit Button
Uses `fetch()` to send POST request to `http://localhost:8000/register` with form data.

> lines 9-11, 27-33